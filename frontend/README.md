# Frontend

A separate web frontend is optional for this hackathon and is currently not implemented.

The Flutter project already includes Flutter web support under:

```text
mobile/ProFixer/web
```

If the project later needs a dedicated web app, it can be added in this `frontend/` folder without mixing it into the backend or mobile source trees.
